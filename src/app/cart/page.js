import styles from "../page.module.scss";

const Cart = () => {
    return (
        <section>
            <h2 style={{ color: "red", textAlign: "center" }}>Корзина товарів</h2>
        </section>
    );
};

export default Cart;
